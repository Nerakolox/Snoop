# Snoop v2 交互重构 · 工单（修订版）

> 本文取代原方案第二部分，是**总纲与排期**。每批的详细执行文档另开单文件，见下表。
> 原型 `snoop-v2-prototype.html` 是**交互契约**（抄行为不抄实现），与本文冲突时以本文为准。

## 进度

| 批次 | 状态 | 详细文档 |
|---|---|---|
| Batch 0 · 盘点 | ✅ 完成 | — |
| Batch 0.5 · 修 key_details N+1 | ✅ 完成（`f9a6c8a`，已人工验证） | 见下方存档 |
| Batch 1 · 地基 | ⬜ 待做 | **`docs/v2-batch1-地基.md`** |
| Batch 2 · 概览 | ⬜ | 开工前另写 |
| Batch 3 · 时间线 | ⬜ | 开工前另写 |
| Batch 4 · 输入 | ⬜ | 开工前另写 |
| Batch 5 · 规律 | ⬜ | 开工前另写 |
| Batch 6 · 收尾 | ⬜ | 开工前另写 |

> **详细文档的写法**：每批开工前，由总管照 Batch 1 那份的详细度另写一份单批文档——把要改的现有代码原文抄进去、把易错点写死、给完工检查表。本总纲只写「做什么」和「为什么」，不写「怎么做」。

---

## 相对原方案的五处修订（背景，已生效）

1. **时间模型从枚举改为语义式** `(kind, anchor)`。原 `RangeKey = 'today'|'yesterday'|'week'|'month'` 表达不了「下钻到 8 月 3 日」和「翻到上上周」，废弃。
2. **顶栏形态随之改变**：不是四段范围切换，而是**粒度三段（日/周/月）+ anchor 导航（← 日期 →）+ 回到今天**。`TimelineHeader` 的日期导航与 `src/components/insights/WeekSelector.tsx` 被它吸收，两者在后续批次中**删除**而非改造。
3. **新增 Batch 0.5**：修 `key_details` 的 N+1。已完成。
4. **四个全站共享件并入 Batch 1**：上下文 chips、toast、悬停导轨、DeltaBadge。原方案把它们埋在 Batch 2，会导致 2/4/5 并行时各造一份。
5. **Windows TitleBar 合并进 TopBar**，`TitleBar.tsx` 删除，`--titlebar-height` 变量删除。否则 Windows 下 36+46=82px 双层顶栏。

---

## 全局约束（每批都适用）

- 每批开始前 `git commit`，每批结束**停下等人工验证**，不要连做。
- 不新增数据采集。所有后端改动只是给现有 SQL 加 `WHERE` 或新增聚合查询，不动表结构。
- **不得回退既有技术结论**：
  - 键盘页 `KEY_UNIT` / `clientWidth` 测量 / `Math.floor` 量化+死区 / RAF 合并 / ResizeObserver 观察非滚动父元素
  - `src/analytics/intensity.ts` 是强度单一真相，禁止组件内自算线性归一化
  - timeline 样式严格隔离在 timeline 相关文件内
  - `CLAUDE.md` 的 PageShell 约定（页面根 class 绝不写 padding）
- **日期处理**：anchor 是 `'YYYY-MM-DD'` 本地日期串。禁止 `new Date("2026-08-03")`（ISO date-only 按 UTC 解析，会漂一天）与 `toISOString()`（同样是 UTC）。统一走 `src/data/ranges.ts` 的 `parseAnchor` / `formatAnchor`。
- **深色模式**：`src/main.tsx:20-26` 恒会给 `<html>` 设 `data-theme`。新 CSS 只写 `[data-theme="dark"]`，**不要写 `@media (prefers-color-scheme: dark)`**——后者在「系统深色 + 用户手选浅色」时会错误命中。`base.css` 里的 media query 是历史写法，不要复制。
- **设计 token**：间距只有 `--space-1/2/3/4/6/8`（4/8/12/16/24/32px），**没有 `--space-5` 和 `--space-7`**。颜色一律走 `src/styles/tokens.css`，不硬编码。
- 不引入任何新依赖。已有的 `lucide-react` / `@tauri-apps/api` 可以用。
- commit message 用中文，不加署名。

---

## 三条跨批语义（写死，任何一批都不得自行解释）

### 1. `adaptKind` 是读取时投影，不写回全局

页面消费 `adaptKind(page, state)` 的返回值即可，**绝不要在 useEffect 里 `setKind`**。否则「概览(月) → 时间线(降级为日) → 返回」回到概览会变成日，用户会觉得应用把他的选择吃掉了。降级必须**可见**（页头挂警示文案），但**不持久**。

### 2. toast 的撤销 = `back()` 整体回退

不是"只清掉某一个字段"。反例：概览点 Chrome 跳时间线，若撤销只清 appId，用户会停在「时间线 + 无筛选」——一个谁都没要求过的状态。

分项清除是 `ContextChips` 上那个叉的职责，两者不重叠。

推论：**只有走了 `actions.navigate()` 的操作才配弹 toast**（因为只有它压了栈）。顶栏上改范围、改筛选不弹 toast。

### 3. store 只存语义，不存毫秒

store 里是 `(kind, anchor)`，各页用 `toMs(kind, anchor)` 自己派生 `[start_ms, end_ms)`。

时间线额外传 `{ liveEnd: true }` 保留它现有的「今天时 end = 当前时刻」语义（原 `Timeline.tsx:123`）。这个规则只属于时间线，不该污染 store。

---

# Batch 0.5 · 修 key_details 的 N+1（已完成，存档）

## 背景

`src-tauri/src/activity_tracker.rs:21` 的 `MAX_BUCKET_MS = 5000`，桶是 **5 秒粒度**，活跃一天约 5000+ 个桶。

原 `Keyboard.tsx` 的 `loadFilteredData` 在选中具体 App 时，对**每一个桶**串行调用 `get_key_details(bucket_id)`，日范围数千次、周范围上万次串行 IPC。

它没被当成 bug 报出来，是因为 `Keyboard.tsx:144` 的 `refresh()` 里强制 `setAppFilter("all")`——切日/切周/刷新都把 App 选择丢掉，用户很难在这个状态上停留。重构第一件事就是删掉那行（appId 变全局），删掉后地雷起爆。所以必须前置。

## 已完成的改动（`f9a6c8a`）

**后端**
- `get_key_details_in_range` 加 `app_bundle_id: Option<String>`，SQL 用 `(?3 IS NULL OR ab.app_bundle_id = ?3)` 过滤，不动表结构。
- 新增 `get_key_hourly_distribution`（单键按本地整点聚合，时区处理照抄 `get_hourly_activity` 的四步字符串转换）。
- 新增 `get_key_app_distribution`（单键跨 App 聚合，降序）。
- 三者已在 `lib.rs` 的 `generate_handler!` 注册。

**前端**
- `fetchKeyDetailsInRange` 第三参数可选，Dev 页原有单参调用不受影响。
- 新增 `fetchKeyHourlyDistribution` / `fetchKeyAppDistribution` 封装。注意 `keyCode` 类型是 **string**。
- `fetchKeyDetailsOfBucket` 保留（Dev 页展开行还在用）。
- `loadFilteredData` 的逐桶串行循环已删除，两分支合并成一次 `fetchKeyDetailsInRange`。
- `Keyboard.tsx:144` 的 `setAppFilter("all")` **按工单保留**，留给 Batch 4 删。

## 验证结果

人工验证通过，响应速度已从数秒级降到即时。

---

# Batch 1 · 地基：store + 顶栏 + 共享件 + 数据层

> **执行文档：`docs/v2-batch1-地基.md`**（含完整代码骨架、现有代码原文、易错点与完工检查表）

本节只记录总纲层面的决策，具体怎么做看那份文档。

## 范围

四组，各一个 commit：

- **C1** — `src/store/context.tsx` + `ranges.ts` 的 `toMs`/`parseAnchor`/`formatAnchor` + Provider 挂载 + **`App.tsx` 的 page 状态迁入 store**
- **C2** — `TopBar.tsx` + `topbar.css` + 删除 `TitleBar.tsx` + `.app-main` 补 `min-height: 0`
- **C3** — `ContextChips` / `Toast` / `.drillable` 三个共享件 + `DeltaBadge` 重写
- **C4** — `useRangeData` 数据层收敛 + 更新 `CLAUDE.md`

## 盘点后新增的三件事（原方案没有）

### 1. `page` 必须进 store

视图栈的栈项是 `{page, kind, anchor, appId, focusHour, selectedKey}`，`back()` 要能恢复**页面**。而现在 `page` 在 `App.tsx:43` 的 useState 里，store 改不了它，`back()` 就是残废的。

所以 `page` 进 store，App 只保留 `displayed` / `phase` 两个纯动画态，两段式转场（`FADE_OUT_MS=160` → 双 rAF → `FADE_IN_MS=280`）从"点击时触发"改成"监听 `page` 变化时触发"。`handleSelectRef` 可以删掉（actions 引用恒定，不再需要 ref 兜最新回调）。

### 2. 拖拽区是「新增」不是「迁移」

盘点结论：macOS 下右半边窗口**现在完全没有**拖拽区，拖拽全在侧栏（`base.css:188` + `Sidebar.tsx` 各层的 `data-tauri-drag-region`）。

所以顶栏是**新增**拖拽区，**侧栏原有的一律保留不动**。用户已经习惯拖侧栏，动它是纯损失。这条改完，Batch 1 的拖拽风险从"中"降到"低"。

### 3. 预验 `fill` + `--no-header`

`page-shell.css` 里这两个变体**从未同时出现过**（只有 Timeline 用 `fill`、只有 Keyboard 用 `stickyHeader`）。Batch 3 把日期导航搬走后，Timeline 很可能变成这个组合。

Batch 1 里临时试一次并把结论写进 commit message。别留到 Batch 3 那个高风险批次才发现——那时分不清是 PageShell 的锅还是时间线重写的锅。

## 原方案里被推翻的一条

> ~~"若外层用 grid，列定义改 `minmax(0, 1fr)`"~~

本项目外层是 flex（`.app-shell` row + `.app-right` column），没有 grid。`.app-right` 已有 `min-width: 0`，`.sidebar` 是定宽 `flex: 0 0 var(--sidebar-width)`。**横向没有同类问题，跳过这条。** 纵向的 `.app-main { min-height: 0 }` 是真问题，必做。

## 本批边界

各页面**仍用自己的旧状态**，表现应与重构前完全一致，只是多了一条顶栏，且顶栏的操作暂时不驱动任何页面。

---

# Batch 2 · 概览页接入全局上下文 + 全量下钻

```
先 git commit "before overview-v2"。
```

1. Overview 删除 `Overview.tsx:106` 的硬编码 `todayRange()`，改为消费全局 `(kind, anchor, appId)`，走 `useRangeData`。
2. **`Overview.tsx:211` 的 `setInterval(refresh, 30_000)` 加条件**：只有 `kind === 'day'` 且 anchor 是今天时才轮询。看上个月的数据没必要每 30 秒重拉一次。轮询触发时要调 `useRangeData` 的 `invalidate()`，否则会一直读缓存。
3. 页头改造：Overview 现在是 `--no-header` 变体（`Overview.tsx:218` 只传了 className），需要新增 header。标题 + 副标题（`${范围名} · ${天数} 天数据${已筛选后缀}`），`adaptKind` 有 note 时在标题右侧渲染警示，标题下渲染 `<ContextChips show={["app"]} />`。
4. **周期总结卡**从 Insights 迁入。模板参数化，日/周/月都出文案：`${范围}${天数描述}，你活跃了 X，敲下 Y 次按键、点出 Z 次点击，鼠标跑了 W 公里。住得最久的是 <App>（时长）。${判语}`。判语按日均活跃小时数分四档，阈值 9 / 6 / 3。文案沿用现有猫人格口吻。
5. **数字卡可点下钻**：活跃时长 → 时间线；总按键/总点击/鼠标里程 → 输入页。悬停时右上角浮出目标提示。多天范围下活跃时长卡副文案写「日均 X · 分母 N 天」，N 为**有采集数据的天数**（不是自然天数）。
6. **App 排行行可点**：`actions.navigate({ page: 'timeline', appId: 该 App })`——**一次 navigate 同时改两个字段**，不要拆成两步（拆了会压出一个中间态）。已有筛选时非选中行 `opacity: 0.34`（不隐藏）。每行右侧挂 `<DeltaBadge>`。行挂 `.drillable`。
7. **节奏区随粒度换形态**：
   - `day` = 24 格热力条，点 → `navigate({ page:'timeline', focusHour: h })`
   - `week` = 7 天柱状，点 → `navigate({ kind:'day', anchor: 该天 })`
   - `month` = 日历热力，同 week
   - 三种形态共用 `src/analytics/intensity.ts`，**禁止组件内自算归一化**
8. 走了 `navigate` 的跳转弹 toast（撤销 = `back()`）。

## 人工验证

三个粒度各点一遍；anchor 前后翻页数据跟着变；每个可点元素都试；toast 撤销有效；返回按钮能回退；**筛选后概览数字确实变了**——核对分 App 时长之和，不是按比例折算。

---

# Batch 3 · 时间线重做

> **高风险。改动严格隔离在 timeline 相关文件内，不碰任何共享 CSS。**
> **本批内部切三段，每段一个 commit**，别用一个回滚点赌一到两天的工作量。

## 3-A · 坐标映射收敛（纯重构，视觉零变化）

```
先 git commit "before timeline-toX"。
```

当前刻度尺、强度曲线、空白压缩各自算位置，容易漂移。改为：

1. 把当日会话序列构造为 bands 数组，每项 `{type: 'run'|'gap'|'empty', start, end, app?}`。
2. 逐个 band 算宽度：gap 且时长 > 120 分钟时固定 38px 并标记 `compressed`，否则 `时长 × pxPerMin`。
3. 累加得到每个 band 的 x 与画布总宽。
4. 导出 `toX(minute: number): number`，压缩带内的任意时刻返回该带中点。

**刻度尺、强度曲线、焦点线、自动滚动必须全部调用 `toX`，不允许任何地方自己算 x。**

**这一段做完视觉应该零变化**，直接肉眼比对刻度对齐——这是整个 Batch 3 最安全的验证点。过了再往下走。

## 3-B · 空白块改单层覆盖

```
先 git commit "before timeline-gap"。
```

前台 App 互斥，空白是全局状态而非某个 App 的属性；当前在每条泳道内各画一个 gap 是建模错误。

改为：泳道渲染完后，在泳道容器上叠一个绝对定位覆盖层，gap 块画在这一层、贯穿所有泳道高度。覆盖层 `pointer-events: none`，gap 块本身 `pointer-events: auto`（要能 hover 出 tooltip）。

## 3-C · 接全局上下文 + 交互

```
先 git commit "before timeline-ctx"。
```

1. **删除 `TimelineHeader` 的日期导航**（`onPrevDay` / `onNextDay` / `onToday` 及对应的 `goPrevDay` :191 / `goNextDay` :199 / `goToday` :209），已被顶栏 anchor 导航吸收。header 里若只剩「重置视图」，考虑整个 header 去掉走 `--no-header`（Batch 1 已验过 `fill` + `--no-header` 的组合，看那批的结论）。
2. 删除 `selectedDate` state（`Timeline.tsx:68`），kind 只支持 `day`，其余走 `adaptKind` 并在页头显示映射警示。`toMs` 传 `{ liveEnd: true }` 保留活范围语义（替代原 `fetchRange` :123 的手写逻辑）。
3. appId 来自全局：被筛选的泳道正常显示，**其余泳道整体降透明度到 0.12–0.25，不要隐藏**。
4. focusHour 存在时渲染竖直焦点线并自动 `scrollLeft`（目标位置减 320px 留出上下文），位置走 `toX`。
5. 页头渲染 `<ContextChips show={["app","focusHour"]} />`。
6. **强度曲线与突变标记**：曲线为 SVG path，viewBox 宽度 = 标签列宽 + 画布总宽。突变检测 `v > prev * 1.8 && v > max * 0.45`。每个突变点渲染可点标记，点击弹猫的吐槽气泡，文案模板化含具体时刻。
7. **段位可点**：点某个 App 段 → `actions.navigate({ appId, focusHour: 该段起始小时 })`，弹 toast。
8. **底部图例**：挂机（有心跳无输入）/ 压缩空白 (>2h) / 活跃强度曲线。

## 明确不做

不加缩放控件。横向滚动 + >2h 压缩带已覆盖缩放要解决的问题，多一个 zoom 状态就多一处要和 `toX`、自动滚动同步的地方。

## 人工验证

逐项点：泳道、色块、刻度对齐、压缩灰块、tooltip、焦点线位置准确、自动滚动落点合理、筛选后灰化正确、突变标记可点。

**任何一块样式秃了立刻回滚到本段开头的 commit**——那是漏搬或污染了共享 CSS，不要在崩坏状态上继续修。

---

# Batch 4 · 输入页

```
先 git commit "before input-v2"。
```

1. 页面改名「输入」（Input）。`Sidebar.tsx:4` 的 `NavKey` 里 `"keyboard"` → `"input"`、`MAIN_ITEMS` 的 label 改「输入」，`App.tsx` 的 `renderPage` switch、`store/context.tsx` 的 `PAGE_KIND_CAP` key **三处一起改**，漏一处就会 undefined。
2. **删除页内 App 筛选与日期筛选**（`Keyboard.tsx:299-317` 的 `.kb-filter-row--apps`、`:322-366` 的日/周切换与日期导航、`:62-63` 的 `timeFilter`/`selectedDate` state、`:47` 的 `getTimeRange`），改用全局上下文。
   - **同时删除 `Keyboard.tsx:144` 的 `setAppFilter("all")`**——这行是全局化的直接障碍。
   - **保留配列选择器**（KLE 那一行）——那是展示配置不是数据筛选，性质不同。保留自定义配列导入与不支持特性警告。
3. 布局改左右两栏：
   - 左：按键热力，沿用现有渲染器。`KEY_UNIT` / `clientWidth` 测量 / `Math.floor` 量化+死区 / RAF 合并 / ResizeObserver 观察非滚动父元素，**这些机制一律不动**。
   - 右：固定宽约 266px 的侧栏，从上到下三块。
4. **单键下钻**（新）：点任意键 → 右栏面板显示键名 + 当前上下文下总次数、24 根时段分布迷你柱（`fetchKeyHourlyDistribution`）、跨应用 Top 3（`fetchKeyAppDistribution`）。未选中时空态引导。选中的键写进 `store` 的 `selectedKey`，页头 `<ContextChips show={["app","selectedKey"]} />`。
   - **面板固定在右栏，不要在键盘下方展开**——那会导致键盘区高度变化触发 ResizeObserver，可能重新引发缩放抖动。
   - 左右 Shift / Ctrl / Alt 合并计数，右侧那个不可点，tooltip 注明「与左侧合并计数」。
5. **鼠标按钮分布**：`src/components/keyboard/MousePanel.tsx` **已存在（130 行），是升级不是新建**。SVG 俯视图五分区（左/右/中键滚轮/侧键1/侧键2）按频次着色走同一强度函数；下方列表名称 + 条 + 次数，0 次显示「未使用」并降透明度。侧键合计 > 0 时强调「你是会用侧键的少数人，配列里值得给它们留位置」，= 0 时低调提示「可以在配列里隐藏它们」。
6. **键鼠比**（新维度，零采集成本）：`点击数 ÷ 按键数`，数据从 buckets 的 `mouse_left + mouse_right + mouse_middle` 与 `key_total` 直接算。展示为大数字 + 判定标签（>0.25 鼠标型 / <0.08 键盘型 / 其余均衡型）+ 从「偏键盘」到「偏鼠标」的滑块。
   - **滑块位置必须对数映射**：比值跨度约 60 倍（终端类 ~0.03，播放器类 ~1.7），线性映射会把大半应用挤在最左端失去分辨力。用 `clamp((Math.log10(ratio) + 1.6) / 2.2, 0, 1)`，阈值可调但必须是对数。
   - `key_total === 0` 时不要除零，显示「无按键数据」。

## 人工验证

切换应用筛选，键鼠比滑块应明显移动且几个 App 分布在两端之间；**键盘缩放无抖动无闪烁**（重点，因为改了容器结构）；单键下钻数据合理，且单键总次数 = 该键在热力图上的数字。

---

# Batch 5 · 规律页

```
先 git commit "before insights-v2"。
```

1. 页面改名「规律」（Patterns）。同 Batch 4 的三处同步改名（`NavKey` / `renderPage` / `PAGE_KIND_CAP`）。`PAGE_KIND_CAP` 限 `['week','month']`，日粒度走 `adaptKind` 降级。
2. **删除 `src/components/insights/WeekSelector.tsx`** 与 `Insights.tsx:128` 的 `selectedWeekStart` state、`:153/:157` 的 `goPrevWeek`/`goNextWeek`——已被顶栏 anchor 导航吸收（并顺带补上了它现在缺的"回到本周"）。
3. `DeltaBadge` 在 Batch 1 已重写为 `{current, previous, vsLabel, baseThreshold}` 且**单位是分钟**，本批直接用，**不要再造，也不要再自己算百分比**。阈值按粒度取：日 20 / 周 120 / 月 600 分钟。
4. 内容重排：
   - 顶部两张并排卡：活跃对比（大数字 + DeltaBadge + 「日均 X · 分母 = 有采集数据的 N 天」）、键鼠比（大数字 + 判定标签 + 近 14 天迷你柱状趋势）
   - 应用排行 · 环比：每行带 DeltaBadge + `.drillable`，点击 → `actions.navigate({ page:'overview', appId })`
   - 作息画像：星期 × 小时三态网格。活跃格可点 → `navigate({ page:'timeline', focusHour })`；挂机与未采集格不可点。存在应用筛选时在标题右侧标注「不受应用筛选影响」（此视图基于 `get_hourly_heartbeats`，与前台 App 无关）
   - 猫发现的规律：4 张卡 = 标题 + 依据 + 跳转动作。跳转目标必须是**能验证这条结论**的页面与状态
5. 周报卡本页删除（Batch 2 已迁往概览）。

## 人工验证

造几个极端数据（基期为 0、基期极小、变化极小），确认 DeltaBadge 五种分支都正确；作息画像格子跳转落点准确。

---

# Batch 6 · 收尾

```
先 git commit "before ui-polish"。
```

1. **侧栏底部常驻实时区**。`.sidebar-bottom`（`base.css:287-291`）已有 `margin-top: auto` + `border-top` + padding，槽位现成，当前只放了「设置」一项。加入：
   - 猫头 SVG，四种状态对应三种眼睛（睁/眯/闭）
   - 状态标签 + 呼吸圆点，四态：爆肝中 / 正常节奏 / 摸鱼中 / 挂机中
   - 一句吐槽，点击换一句
   - 明标「LIVE · 不随范围变」
   - 注意 `.sidebar-bottom` 整块挂着 `data-tauri-drag-region`，新加的可点元素要排除拖拽
2. **托盘小窗共用状态源**——跨 WebviewWindow 共用意味着实时状态要走 Tauri event 而不是 React state。**这条单独估工，不算在本批的半天里**，若成本超预期就先只做侧栏、托盘另开一批。
3. **交互一致性巡检**：所有可点行用同一套 `.drillable`；所有 tooltip 走同一组件，统一 `data-tt` / `data-tt-title` 约定；所有走 `navigate` 的跳转弹 toast；所有可点元素为真 `button` 或带 `tabindex`，`focus-visible` 有可见轮廓。
4. **空态与降级文案**：每页在「该范围无数据」时给明确空态，说明发生了什么 + 下一步能做什么，不要只显示 0。
5. **回归检查**：逐页对比重构前截图，确认没有样式漏搬。深浅色各扫一遍，且要验「系统深色 + 手选浅色」这个组合。

---

# 依赖链与排期

```
0 ✅ → 0.5 ✅ → 1 → { 2, 4, 5 } → 6
                     └─ 3 插在 2 之后，单独做
```

| 批次 | 工作量 | 风险 | 说明 |
|---|---|---|---|
| ~~0.5 后端修 N+1~~ | ~~半天~~ | — | 已完成 |
| 1 地基 | 一天半 | 中 | 比原方案多半天（吸收共享件 + Windows 顶栏合并 + page 状态迁移） |
| 2 概览 | 一天 | 低 | |
| 3 时间线 | 一到两天 | **高** | 内部三段各自 commit，单人盯，不与别的批次同时改 |
| 4 输入 | 一天 | 中 | 改了键盘容器结构，可能重新触发缩放抖动 |
| 5 规律 | 一天 | 低 | |
| 6 收尾 | 半天 + 托盘另估 | 低–中 | |

**可并行**：Batch 1 落地后，2 / 4 / 5 之间无强依赖（共享件已在 Batch 1 就位，不会撞车）。Batch 3 建议单独做。

**回滚点**：每批（Batch 3 是每段）开头的 commit。
